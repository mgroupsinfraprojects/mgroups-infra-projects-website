# `tests/` — Quality and Regression Suite

This folder protects the application against regression in audit correctness, import formats, UI contract behavior, export output, release packaging, and version-specific fixes.

Run:

```bash
python -m pytest -q
```
