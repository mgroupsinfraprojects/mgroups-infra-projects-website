# Frontend Layer

The actual frontend code is kept in `app/ui/` to preserve the working imports.

Go to:

```text
app/ui/
```

This layer contains:

- main desktop window
- dashboard pages
- upload/audit/export/settings screens
- reusable PySide6 widgets
- guided search and filter popup
- theme manager
- background worker integration
