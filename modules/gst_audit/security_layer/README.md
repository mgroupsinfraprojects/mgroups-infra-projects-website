# Security Layer

Main file:

```text
app/core/security.py
```

This layer contains RBAC roles/permissions, hashing, encryption helpers, and audit-event hash utilities. Database hash-chain verification is integrated through `app/core/database.py`.
