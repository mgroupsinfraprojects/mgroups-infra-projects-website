# Deployment

Deployment-related files:

```text
GSTInvoiceAudit.spec
build_exe.bat
run_app.bat
requirements.txt
pyproject.toml
```

This release includes build helpers, but true enterprise deployment still requires a signed Windows installer, validated EXE, update/rollback policy, and organization-specific security configuration.
